server {

  listen 80 ;
  # listen [::]:80 ;

  server_name backend-xp-dashboard.iconblockchain.xyz;

  location / {
   proxy_set_header Host $host;
   proxy_pass http://rest-server;
   proxy_next_upstream error timeout http_500;
  }
}
